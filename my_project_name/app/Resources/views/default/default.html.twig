<!DOCTYPE html>
<html>
    <head>
        <title>Welcome to Symfony!</title>
    </head>
    <body>
        <h1>学习</h1>
        <center>
        	<table border="2">
        	<a href="{{path ('default_create')}}">添加数据</a>
        		<tr>
        			<td>ID</td>
        			<td>姓名</td>
        			<td>性别</td>
        			<td>年龄</td>
        			<td>操作</td>
        		</tr>
        		{% for item in products %}
        		<tr>
        			<td>{{item.id}}</td>
        			<td>{{item.name}}</td>
        			<td>{{item.sex}}</td>
        			<td>{{item.age}}</td>
        			<td><a href="{{path ('default_delete' ,{productId:item.id})}}">删除</a>||<a href="{{path ('default_update' ,{productId:item.id})}}">编辑</a></td>
        		</tr>
        		{% endfor %}
        	</table>
        </center>
    </body>
</html>